appId: com.dmarket.bot
productName: DMarket Bot
directories:
  output: dist-electron
files:
  - main/**
  - renderer/dist/**
  - package.json
  - shared/**
extraMetadata:
  main: main/main.js
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  createDesktopShortcut: true
  createStartMenuShortcut: true
win:
  target:
    - target: nsis
      arch:
        - x64
  # icon: build/icon.ico  # Uncomment if you have an icon file
mac:
  # Use universal build - works on both Intel (x64) and Apple Silicon (arm64)
  target:
    - target: dmg
      arch:
        - universal
  # Alternative: separate builds for each architecture (if universal doesn't work)
  # target:
  #   - target: dmg
  #     arch:
  #       - x64
  #       - arm64
  category: public.app-category.utilities
  hardenedRuntime: false
  gatekeeperAssess: false
  identity: null
  entitlements: null
  entitlementsInherit: null
  # icon: build/icon.icns  # Uncomment if you have an icon file
linux:
  target: AppImage
  # icon: build/icon.png  # Uncomment if you have an icon file

# Disable automatic publishing to GitHub
publish: null


